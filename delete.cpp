#include <bits/stdc++.h>
using namespace std;
int fx(vector<int> &arr){
    int n=arr.size();
    if(n<=1) return 0;
    if(arr[0]==0) return -1;
    int l=0;
    int r=0;
    int jumps=0;
    while(r<n-1 && r>=l){
        int temp=r+1;
        for(int i=l;i<=r;i++) r=max()
    }
}
int main(){
    vector<int> arr={1,3,5};
    
}

