#include <bits/stdc++.h>
using namespace std;

int main(){
    return 0;
}

int fx(vector<int> &arr){
    int n=arr.size();
    vector<vector<int>> dp(n+1,vector<int> (n+1,-1));
    for(int i=)
}