#include <bits/stdc++.h>
using namespace std;

bool fx(vector<int> &arr,int k){
    int n=arr.size();
    vector<vector<int>> dp(n,vector<int> (k+1,0));
    for(int i=0;i<n;i++) dp[i][0]=true;
    dp[0][arr[0]]
}
int main(){
    return 0;
}